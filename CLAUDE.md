# Lexab (repo: Civis)

Legal AI SaaS: анализ договоров с привязкой к законодательству.
Юрисдикции: 7 живых — UK, Узбекистан, Казахстан, Германия, США (федеральное),
Канада (Квебек), ОАЭ. Детали и метрики — в «Статус этапов RAG» ниже.

## Стек

- Фронтенд: React 18 + TypeScript + Vite (корень репозитория), порт 5173 (strictPort).
- Бэкенд: Fastify 5, Node 24 c нативным запуском TS (`server/`), порт 8080.
- База: Supabase Postgres + pgvector (миграции `server/migrations/*.sql`,
  применяются автоматически при старте сервера).
- ИИ: модель зависит от тарифа (`server/src/config.ts` → `planModels`).
  Платные планы — Anthropic API; Free — DeepSeek (OpenAI-совместимый клиент,
  включается ключом `DEEPSEEK_API_KEY`, id модели с «deepseek» маршрутизируется
  в него — `server/src/llm.ts`), при сбое ретрай на Anthropic. Эмбеддинги —
  Voyage AI `voyage-law-2` (RAG-поиск от генеративной модели не зависит).

## Запуск

```bash
cd server && npm run dev     # API на :8080 (авто-миграции)
npm run dev                  # фронтенд на :5173 (в корне)
```

Секреты — ТОЛЬКО в gitignored `.env` / `server/.env` (см. `server/.env.example`).

## Legal RAG — критические правила

1. **НИКОГДА не генерировать текст законов.** Тексты правовых норм попадают в
   корпус только из официальных источников. Если источник недоступен —
   документ помечается `status='missing'`; восстанавливать «по памяти»
   запрещено. Сгенерированный «текст закона» в юридическом продукте — worst
   case failure.
2. **Белый список источников:** legislation.gov.uk (UK законы),
   caselaw.nationalarchives.gov.uk (UK практика), lex.uz (UZ),
   adilet.zan.kz (KZ), gesetze-im-internet.de (DE — офиц. база
   Bundesministerium der Justiz), govinfo.gov (US — федеральный United States
   Code от GPO), legisquebec.gouv.qc.ca (CA — офиц. свод законов Квебека,
   Publications Québec), uaelegislation.gov.ae (AE — офиц. портал
   законодательства ОАЭ; живой хост за Cloudflare-челленджем, поэтому берётся
   точный снимок офиц. страницы из web.archive.org — см. `ingest/uaelegislation-ae.ts`).
   Больше НИОТКУДА.
3. **Provenance обязателен** — enforced CHECK-констрейнтами (`source_url`,
   `sha256_checksum`, `retrieved_at`); строка без источника не вставится.
4. **Цитаты валидируются в коде, не в промпте** —
   `server/src/rag/validate-citations.ts`: находка без подтверждённой ссылки
   автоматически понижается и помечается `unverified: true`.
5. **Любое изменение ранжирования, промптов или индекса — обязательный прогон
   `npm run eval` до и после.** Отчёты в `server/evals/reports/`.

Схема корпуса и пайплайны: `docs/corpus-schema.md`. Статус: `HANDOFF.md`.

## Команды RAG (в `server/`)

```bash
npm run rag:ingest      # загрузка актов UK (идемпотентно; --force для перепарсинга)
npm run rag:ingest:uz   # загрузка законов РУз с lex.uz
npm run rag:ingest:kz   # загрузка законов РК с adilet.zan.kz
npm run rag:ingest:de   # загрузка законов ФРГ с gesetze-im-internet.de (BGB)
npm run rag:ingest:us   # загрузка фед. законов США с govinfo.gov (FAA, E-SIGN)
npm run rag:ingest:ca   # загрузка ГК Квебека с legisquebec.gouv.qc.ca (CCQ, обязательства)
npm run rag:ingest:ae   # загрузка ГК ОАЭ (офиц. англ. перевод, снимок офиц. страницы)
npm run rag:index       # статьи → чанки + ИИ-контекст (--docs=… --max-annotate=N
                        # --annotate-model=deepseek-v4-pro для дешёвых аннотаций;
                        # ВСЕГДА с --docs=, иначе дообогатит и чужие юрисдикции)
npm run rag:embed       # векторы (нужен VOYAGE_API_KEY; при 429 на жирных пакетах:
                        # EMBED_BATCH=2 EMBED_SLEEP_MS=21000 npm run rag:embed)
npm run eval            # метрики (--golden=uz-contract-law.jsonl для УЗ, --limit N;
                        # RAG_VALIDATE_MODEL=deepseek-v4-pro — судья цитат на DeepSeek)
node --env-file=.env scripts/rag-coverage.ts UZ   # покрытие + линт golden
```

**Политика eval-отчётов:** отчёт цитируем ТОЛЬКО при `rag-coverage` exit 0
(100% чанков/аннотаций/векторов по юрисдикции) — иначе метрики занижены и
несравнимы. DeepSeek v4 — рассуждающая модель: в микрозапросах (аннотации,
судья цитат) размышления отключаются `thinking:{type:'disabled'}`, иначе весь
max_tokens уходит в раздумья и content пустой.

## Статус этапов RAG

- Этап 1 (схема, temporality, provenance) — ✅
- Этап 2 (ingestion UK, 8 пилотных актов) — ✅ (schedules и исторические
  редакции — позже)
- Этап 3 (чанки-статьи, гибридный retrieval, RRF, векторы voyage-law-2 на
  всём корпусе, cross-encoder reranker Voyage rerank-2.5-lite fail-open) — ✅
- Этап 4 (unit_id в находках + валидатор цитат, RU+EN форматы цитат,
  UI-бейдж «Проверено по базе законов» на находках) — ✅
- Этап 5 (eval-harness: golden UK 18 вопросов + golden UZ 14 вопросов) — ✅
- **Узбекистан — ✅ LIVE, волны 1-5 (2026-07-28): 47 документов, 5724 статьи —
  договорное ядро ЗАКРЫТО.** Волна 5: Жилищный кодекс (141 ст.),
  Об электроэнергетике ЗРУ-939 2024 (68), Об аудиторской деятельности ЗРУ-677
  (55), О гарантиях свободы предпринимательской деятельности 69-II (51),
  О небанковских кредитных организациях и микрофинансовой деятельности ЗРУ-765
  (45), О лизинге 756-I (25), О внешнеэкономической деятельности 285-XII (20).
  Golden 97→107 (retrieval новых 14/14). Финальный eval: retrieval 1.0/1.0/1.0,
  цитаты DeepSeek 27.3%→100%, unverified 0, покрытие 100%. Суммарный расход
  DeepSeek на волны 3-5 (1934 аннотации + 3 eval) ≈ $0.30.
  Волна 4 (банки/споры/стройка): Об исполнении судебных актов 258-II (118 ст.),
  О нотариате 343-I (113), Градостроительный кодекс ЗРУ-676 (85), О платежах
  и платёжных системах ЗРУ-578 (63), О рынке ценных бумаг ЗРУ-163 (62),
  О третейских судах ЗРУ-64 (60), О межд. коммерческом арбитраже ЗРУ-674 (56),
  О банках 216-I (45), О ГЧП ЗРУ-537 (45), О бухучёте 279-I (24) — ~663 ст.
  «О естественных монополиях» НЕ взят — утратил силу 2023 (заменён ЗРУ-850).
  Golden 83→97 (+14, retrieval 14/14 ранг 1). Смоуки: резолвер 9/10 (десятый —
  реально исключённая ст.25 нотариата, fail-closed корректен), retrieval 10/10
  ранг 1. Аннотации 663/663 deepseek-v4-pro (313k/52k ток.), вектора 100%.
  Волна 3 (договорное ядро): Таможенный кодекс (419 ст.), О страховой
  деятельности (ЗРУ-730), Об авторском праве (ЗРУ-42), О рекламе (ЗРУ-776),
  О техническом регулировании (ЗРУ-819), О транспорте (ЗРУ-706),
  Об изобретениях (1062-XII), О товарных знаках (267-II), О персональных
  данных (ЗРУ-547), Об электронной коммерции (ЗРУ-792) — ~866 статей.
  Golden 69→83 (+14 по новым актам, retrieval 14/14 в топ-10). Метрики после
  волны 3: retrieval 1.0/1.0/1.0, цитаты DeepSeek 45.5%→100%, unverified 0.
  Резолвер цитат детерминирован (ORDER BY специфичность заголовка вместо
  произвольного LIMIT 1 — критично при 30+ актах) + DB-тест коллизий.
  Волна 2: Налоговый кодекс 2019 (500 ст.), Земельный кодекс (110 ст.; 15
  «дыр» в нумерации = реально исключённые статьи, в т.ч. 92–100 — земельная
  реформа 2021), О валютном регулировании (841-XII), О конкуренции (ЗРУ-850,
  49 ст.), О лицензировании (ЗРУ-701), О неплатежеспособности (ЗРУ-763 —
  сменил «О банкротстве»), О госзакупках (ЗРУ-684), Об инвестициях (ЗРУ-598).
  Golden 47→69 (+18 по волне 2 + 4 узбекоязычных, все через агентов-скептиков
  по текстам из БД). Метрики волны 2 (реранкер живой): hit@5 0.986 (68/69),
  hit@10 1.0; узб. вопросы 4/4 в топ-10 (приёмка ≥0.8); цитаты DeepSeek
  6.8%→100%. REWRITE_SYSTEM.UZ: вопрос может быть на узбекском/английском —
  запросы всегда по-русски. Судья цитат понимает логику ревю (находка цитирует
  норму, которой пункт договора ПРОТИВОРЕЧИТ — это supported=true).
- Волна 1 (2026-07-18): 12 документов, 2625 статей
  (lex.uz: ГК ч.1+2, №670-I о договорно-правовой базе, ЗРУ-793 об ЭЦП,
  о защите прав потребителей + Трудовой кодекс 2022 (581 ст.), О залоге,
  Об ипотеке, Об ООО (НОВЫЙ ЗРУ-1137 от 21.04.2026 — сменил 310-II), Об АО,
  ЭПК 2018 (390 ст.), Об аренде; русская морфология FTS — миграция 015).
  Аннотации 100% (deepseek-v4-pro, thinking off), векторы 100%.
  Метрики UZ (golden 47 вопросов, DeepSeek): точность цитат 5.7% без RAG →
  100% с RAG, unverified 0; retrieval hit@5 0.936→(после правки метки) выше,
  hit@10 1.0, recall@10 0.989. Резолвер цитат: parseRuCitation + алиасы
  (ГК/ТК/ЭПК/ипотека/залог/ООО/АО/аренда/ЗоЗПП/ЭЦП, юникод-границы — JS \b
  кириллицу не видит), KZ намеренно без изменений. Golden 14→47 (сценарные/
  ловушки/межактовые; каждый вопрос написан по тексту статьи из БД и проверен
  скептиком). Отложено: узбекоязычные вопросы (фаза 5), волна 2 актов.
- **Казахстан — ✅ LIVE** (adilet.zan.kz: ГК Общая + Особенная части,
  о защите прав потребителей, об ЭЦП — 4 документа, 1249 статей, русская FTS
  как у UZ). Парсер `ingest/adilet-kz.ts` (fetch через curl — сайт рвёт undici;
  два стиля разметки: `<h3>` в кодексах и `<p><b>Статья N.</b></p>` в новых
  законах). Метрики KZ: retrieval hit@5 0.86–0.93, hit@10 1.0; точность цитат
  DeepSeek 58.6% без RAG → 100% с RAG. Аннотации НЕ строились (бюджет) — качество
  на голом тексте статьи + вектор + реранкер.
- **Германия — ✅ LIVE** (gesetze-im-internet.de: BGB, 2510 §; парсер
  `ingest/gesetze-de.ts` — XML из xml.zip, встроенная распаковка; немецкий
  стеммер FTS — миграция 023; цитаты «§ 433 BGB»). Метрики DE: retrieval
  hit@5 0.938, hit@10 0.938; точность цитат DeepSeek 97.6%→100% (BGB хорошо
  известен модели — ценность RAG здесь в проверенных ссылках на источник).
- **США (федеральное) — ✅ LIVE** (govinfo.gov: US Code Title 9 FAA арбитраж +
  Title 15 гл.96 E-SIGN эл. подписи; 41 §. Парсер `ingest/govinfo-us.ts` —
  официальный PDF от GPO через pdf-parse, отсекает редакц. примечания по
  credit-line; цитаты «9 U.S.C. § 2». Миграция 024). Метрики US: retrieval
  hit@5 1.0; точность цитат DeepSeek 81.8%→96.4%. **ЯДРО договорного права США
  (UCC Статья 2) — право штатов; НЕ включено:** канонические офиц. источники
  (uscode.house.gov XML, NY OpenLegislation) недоступны из этой сети, сайты
  штатов рендерятся JS, а зеркала (Cornell/Justia) — не официальные. Добавить,
  когда появится офиц. машиночитаемый источник штата.
- **Канада — ✅ LIVE** (legisquebec.gouv.qc.ca: Гражданский кодекс Квебека
  CCQ-1991, Книга Пятая «Обязательства» — договорное право ст. 1371–2643,
  1271 статья. Парсер `ingest/legisquebec-ca.ts` — HTML (fetch через curl с
  браузерным UA, иначе 403); цитаты «art. 1385 CCQ»; миграция 025). Метрики CA:
  retrieval hit@5 1.0; точность цитат DeepSeek 92.3%→100%. Квебек = гражданское
  право (кодифицировано); общее право остальных провинций (прецеденты) — вне
  корпуса статутов.
- **ОАЭ — ✅ LIVE** (uaelegislation.gov.ae: Гражданский кодекс — Federal Law
  No. 5 of 1985 Civil Transactions Law, офиц. английский перевод Минюста,
  1526 статей; арабский НЕ нужен — english FTS. Живой портал за
  Cloudflare-челленджем → парсер `ingest/uaelegislation-ae.ts` берёт точный
  снимок офиц. страницы из web.archive.org; цитаты «Article 125 Civil
  Transactions Law»; миграция 026). Метрики AE: retrieval hit@5 0.875;
  точность цитат DeepSeek 50%→96.9% (+46.9 п.п. — крупнейший прирост:
  модели плохо знают ГК ОАЭ). ВСЕ СТРАНЫ ПЛАНА ЗАКРЫТЫ: 7 юрисдикций живые.
- RAG подключён и к анализу (топ-10), и к чату (топ-5 по юрисдикции чата).
- Отложено: UCC Статья 2 (ядро договорного права США — право штатов) — когда
  появится офиц. машиночитаемый источник штата.
- **Реранкер и eval:** на бесплатном тарифе Voyage реранкер и векторизация
  запроса упираются в лимит запросов при батч-прогоне eval (429). Для честного
  замера в eval есть `RERANK_MAX_RETRIES` (по умолч. 0 — прод не трогаем: там
  запросы идут по одному и реранкер работает). Флаг `--limit N` в run-eval.ts —
  дешёвая выборка вопросов для платных моделей.

## Правила работы

- Всё проверять реальным запуском; не отчитываться о непроверенном.
- Моки — только в unit-тестах.
- Тесты: `npm test` в корне (vitest) и в `server/` (node:test).
- Тестовые пользователи в базе создаются только на время проверки и
  удаляются через DELETE /api/me.
